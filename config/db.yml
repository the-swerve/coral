development:
  sessions:
    default:
      database: 'dev'
      hosts:
        - localhost:27017
production:
  sessions:
    default:
      database: <%= URI.parse(ENV['MONGOHQ_URL']).path.gsub(/^\//, '') %>
      hosts:
       - <%= URI.parse(ENV['MONGOHQ_URL']).host + ':' + URI.parse(ENV['MONGOHQ_URL']).port.to_s %>
      username: heroku
      password: 'deep sea creature'
