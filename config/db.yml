development:
  sessions:
    default:
      database: 'dev'
      hosts:
        - localhost:27017
production:
  sessions:
    default:
      uri: <%= ENV['MONGOHQ_URL'] %>
      username: heroku
      password: deep sea creature
