development:
  sessions:
    default:
      database: 'dev'
      hosts:
        - localhost:27017
production:
  sessions:
    default:
      host: <%= URI(ENV['MONGOHQ_URL']).host %>
      port: <%= URI(ENV['MONGOHQ_URL']).port %>
      username: heroku
      password: deep sea creature
